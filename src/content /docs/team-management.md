## Team Memberships

When adding a member to a team, the system automatically assigns a role based on the user's organization-level permissions if no specific role is provided:

- Organization **ADMIN** users are assigned the **ADMIN** role on the team by default.
- Other users are assigned the **REVIEWER** role by default.

You can override this default behavior by explicitly specifying a role when adding the member.